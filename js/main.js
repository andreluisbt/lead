$("document").ready(function(){

	$('.pie-chart').easyPieChart({
		barColor: '#009ee3',
		trackColor: '#f2f2f2',
		trackWidth: 20,
		scaleColor: false,
		size: 230,
		lineCap: 'square',
		lineWidth: 25,
	});
});